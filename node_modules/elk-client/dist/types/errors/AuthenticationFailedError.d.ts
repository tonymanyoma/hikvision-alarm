export declare enum AuthenticationFailedReason {
    Unknown = 0,
    InvalidCredentials = 1,
    MissingUsername = 2,
    MissingPassword = 3
}
/**
 * Inidicates that an authentication error occured. Either
 * authentication failed, or there was no authentication
 * information provided.
 */
export default class AuthenticationFailedError extends Error {
    readonly reason: AuthenticationFailedReason;
    constructor(reason?: AuthenticationFailedReason, message?: string);
}
