export default class UserCode {
    readonly code: string;
    readonly isProximityCard: boolean;
    static decode(input: string): UserCode;
    constructor(code: string, isProximityCard?: boolean);
    encode(width?: number): string;
}
