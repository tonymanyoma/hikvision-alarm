declare enum ArmingLevel {
    Disarm = 48,
    ArmedAway = 49,
    ArmedStay = 50,
    ArmedStayInstant = 51,
    ArmedNight = 52,
    ArmedNightInstant = 53,
    ArmedVacation = 54,
    ArmToNextAwayMode = 55,
    ArmToNextStayMode = 56,
    ForceArmToAwayMode = 57,
    ForceArmToStayMode = 58
}
declare namespace ArmingLevel {
    const AWAY_LEVELS: ArmingLevel[];
    const NIGHT_LEVELS: ReadonlyArray<ArmingLevel>;
    const STAY_LEVELS: ReadonlyArray<ArmingLevel>;
    const INSTANT_LEVELS: ReadonlyArray<ArmingLevel>;
    const FORCE_LEVELS: ReadonlyArray<ArmingLevel>;
    const VACATION_LEVELS: ReadonlyArray<ArmingLevel>;
    function isAway(armingLevel: ArmingLevel): boolean;
    function isNight(armingLevel: ArmingLevel): boolean;
    function isStay(armingLevel: ArmingLevel): boolean;
    function isInstant(armingLevel: ArmingLevel): boolean;
    function isForce(armingLevel: ArmingLevel): boolean;
    function isVacation(armingLevel: ArmingLevel): boolean;
}
export default ArmingLevel;
